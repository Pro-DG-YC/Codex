# Codex

## Previewing the site locally

To review the current design in a browser, start a simple local server from the project root:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000/` in your browser for a live preview.